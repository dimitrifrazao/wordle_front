export enum KeyColor {
  grey = "grey",
  green = "green",
  yellow = "yellow",
  filled = "filled",
  empty = "empty",
}

export interface Letter {
  key: string;
  color: KeyColor;
}

export interface Letter {
  key: string;
  color: KeyColor;
}
